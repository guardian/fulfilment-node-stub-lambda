{
  "name": "fulfilment-node-stub-lambda",
  "version": "0.0.1",
  "description": "hello world lambda that deploys through riff-raff",
  "main": "hello.js",
  "isAwsLambda": true,
  "cloudformation": "cloudformation.yaml",
  "projectName": "MemSub::Fulfilment::Test",
  "riffraffFile": "riff-raff.yaml",
  "cloudformationAppTag": "fulfilment-node-stub-cloudformation",
  "buildDir": "dist",
  "devDependencies": {
    "@guardian/node-riffraff-artifact": "^0.0.15",
    "@zeit/ncc": "^0.20.4",
    "lambda-local": "^1.6.3"
  },
  "scripts": {
    "compile": "ncc build src/hello.js -o dist -m",
    "dist": "yarn install --production --modules-folder dist/node_modules",
    "riffraff": "node-riffraff-artifact",
    "runLocal": "./runLocal.js",
    "clean": "rm -rf dist && rm -rf target"
  },
  "dependencies": {
    "lodash-es": "^4.17.4"
  }
}
