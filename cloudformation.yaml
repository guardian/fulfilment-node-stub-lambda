AWSTemplateFormatVersion: "2010-09-09"
Description: hello world lambda that deploys through riff-raff

Parameters:
    DistBucket:
        Description: The S3 Bucket for distributing code in this account
        Type: String
    App:
        Description: Application name
        Type: String
        Default: node-stub-lambda
    Stack:
        Description: Stack name for the account which will hold this lambda
        Type: String
    Stage:
        Description: Stage name
        Type: String
        AllowedValues:
            - PROD
            - CODE
        Default: CODE

Resources:
    NodeStubLambdaRole:
        Type: AWS::IAM::Role
        Properties:
            RoleName: !Sub NodeStubLambda-${Stage}
            AssumeRolePolicyDocument:
                Statement:
                    - Effect: Allow
                      Principal:
                          Service:
                             - lambda.amazonaws.com
                      Action:
                          - sts:AssumeRole
            Path: /
            Policies:
                - PolicyName: LambdaPolicy
                  PolicyDocument:
                      Statement:
                          - Effect: Allow
                            Action:
                            - logs:CreateLogGroup
                            - logs:CreateLogStream
                            - logs:PutLogEvents
                            - lambda:InvokeFunction
                            Resource: "*"

    NodeHelloLambda:
        Type: AWS::Lambda::Function
        Properties:
            Description: hello world lambda that deploys through riff-raff
            FunctionName:
                !Sub node-hello-lambda-${Stage} # TODO: edit this to name your lambda
            Code:
                S3Bucket: !Sub ${DistBucket}
                S3Key: !Sub ${Stack}/${Stage}/${App}/${App}.zip
            Handler: hello.handler # TODO: the name of the function in the code which implements your lambda
            Role:
                Fn::GetAtt:
                - NodeStubLambdaRole
                - Arn
            MemorySize: 512
            Runtime: nodejs10.15
            Timeout: 300
            Environment:
                Variables:
                    'Stage': !Sub ${Stage}
        DependsOn:
        - NodeStubLambdaRole
