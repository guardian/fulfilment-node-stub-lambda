regions:
- eu-west-1
stacks:
- <STACK> 
deployments:
  node-lambda:
    type: aws-lambda
    parameters:
      bucket: <BUCKET-NAME> # typically your "-dist" bucket
      prefixStack: false
      fileName: 'node-lambda.zip'
      functionNames:
      - node-hello-lambda-
#  fulfilment-node-stub-cloudformation:
#  N.B.: cloudformation deployment not quite finished
#   type: cloud-formation
#     parameters:
#       templatePath: 'cloudformation/cloudformation.yaml'
