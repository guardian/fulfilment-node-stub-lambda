regions:
- eu-west-1
stacks:
- membership
templates:
  lambda:
    type: aws-lambda
    parameters:
      bucket: fulfilment-node-stub-lambda-dist
      functions:
        CODE:
          name: fulfilment-node-stub-lambda-dist-CODE
          filename: fulfilment-node-stub-lambda-dist.zip
        PROD:
          name: fulfilment-node-stub-lambda-dist-PROD
          filename: fulfilment-node-stub-lambda-dist.zip
      functionNames:
      - fulfilment-node-stub-lambda-
deployments:
  fulfilment-node-stub-lambda:
    template: lambda
